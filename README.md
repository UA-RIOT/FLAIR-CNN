# FLAIR

Flow-Level Autoencoder for Intrusion Recognition.

This repository contains the codebase for the FLAIR model.

To download the correct dependencies run:
python -m pip install -r requirements.txt
